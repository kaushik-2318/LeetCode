SELECT t1.project_id, ROUND(SUM(t2.experience_years)/COUNT(t1.project_id),2) as average_years  FROM Project as t1 LEFT JOIN Employee as t2 ON t1.employee_id = t2.employee_id GROUP BY t1.project_id order by t1.project_id asc;